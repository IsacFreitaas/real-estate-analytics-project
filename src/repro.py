RANDOM_STATE = 42
"""
Single source of truth for the project's random seed.

Used by train/test splitting, cross-validation, and every stochastic
estimator (e.g., RandomForestRegressor), so results are reproducible across
runs and consistent across the whole pipeline.
"""
